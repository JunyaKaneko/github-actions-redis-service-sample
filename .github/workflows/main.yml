on: [push]

jobs:
  main-job:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.5, 3.6]

    services:
      redis:
        image: redis

    steps:
    - uses: actions/checkout@v1